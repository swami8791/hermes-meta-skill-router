# Pin the rule set so `make lint` means the same thing on every Ruff version.
# These are Ruff's pre-0.16 defaults; newer releases enable more rules by default.
[lint]
select = ["E4", "E7", "E9", "F"]
